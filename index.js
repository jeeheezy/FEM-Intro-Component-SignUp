const claim = document.querySelector('.submit');

function checkEmpty() {
  let inputs = document.querySelectorAll('input[type=text]');
  let emptyflags;
  emptyflags= inputs.forEach(input => w)
  if () {

  }
}

claim.addEventListener('submit', checkEmpty);